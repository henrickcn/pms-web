requirejs.config({
    baseUrl : "./",
    paths: {
        core   : "core/base",
        config : "config/webconfig",
        helper: "core/helper/helper"
    }
});

requirejs(['core'], function (core) {
    
});